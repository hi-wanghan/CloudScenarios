@model IEnumerable<ArenaTest.Models.TestRun>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>

    @Html.ActionLink("Create New", "Create")

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Platform)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TestId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Platform)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TestId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TestRunId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TestRunId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TestRunId })
        </td>
    </tr>
}



</table>
